name: Validate pets

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Check generated previews
        run: python scripts/generate_previews.py --check

      - name: Check generated README files
        run: python scripts/generate_readmes.py --check

      - name: Validate pet packages
        run: python scripts/validate_repo.py
